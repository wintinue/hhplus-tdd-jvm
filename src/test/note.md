더미 데이터 있는 db 필요
stub, mock는 어떻게 쓰는 거지?

공통 로직
a. 존재하는 유저 포인트인지 확인
있으면 유저 포인트 리턴
없으면 exception 발생

1. 포인트 조회
   2. a. 존재하는 유저 포인트인지 확인
   3. exception 발생 시 404 에러 리턴
2. 포인트 내역 조회
   3. a.
   4. exception 발생 시 404 에러 리턴
   4. updateMillis DESC로 정렬해서 리턴
3. 포인트 충전
   4. a. 존재하는 유저 포인트인지 확인
   5. exception 발생 시 404 에러 리턴
   6. db에 유저 포인트 amount 더하여 데이터 업데이트
   7. 
4. 포인트 사용